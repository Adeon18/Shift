//! doctest entry point for shift_tests. Keep this  free of engine includes so the
//! generated main() compiles in isolation and stays fast to rebuild.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
